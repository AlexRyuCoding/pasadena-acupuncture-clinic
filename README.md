# Pasadena Acupuncture Clinic — Demo Site
Concept demo by The Practice Factor. Static single-page site (GSAP + ScrollTrigger
+ Lenis via CDN, no build step). Open `index.html` or serve with
`python3 -m http.server`. Deployed to GitHub Pages from `main` / root.
**This is a demonstration, not the clinic's live website.**
